//Assigning elements to variables
const cookie = document.getElementById("bigCookie")
function click(button,x)
{
    for (let i=0; i<x; i++)
    {
        button.click()
        setTimeout(10, a)
    }
}